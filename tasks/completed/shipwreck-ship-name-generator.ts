/**
 * Task pointer: the shipwreck vessel name generator lives in
 * `src/utils/shipwreckShipNameGenerator.ts` and is re-exported from `src/utils/nameGenerator.ts`
 * as `generateShipwreckName` and `generateShipwreckShipName`.
 *
 * Design goals (poetic ship names, not hull/bilge debris labels, nautical words occasional, etc.)
 * are implemented there.
 */
export {}
