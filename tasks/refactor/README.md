# Refactor

This folder holds specs and notes for codebase refactoring work.

- **refactor-spec.md** – Main refactor specification: dimension/spawn/advanced helpers, EditMode factory, ExportPanel/MainApp/useRegions splits, optional context split, and hygiene. Work is ordered in phases (quick wins → medium → larger).

Use the spec as the single source of truth when planning or implementing refactors.
